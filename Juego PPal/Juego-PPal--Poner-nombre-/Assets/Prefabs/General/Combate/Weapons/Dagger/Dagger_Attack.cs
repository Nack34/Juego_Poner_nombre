using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Dagger_Attack : MonoBehaviour // aca se hace el OntriggerEnter y se manda la senial a la collision ()
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
